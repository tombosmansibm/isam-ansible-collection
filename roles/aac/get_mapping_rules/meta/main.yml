---
galaxy_info:
  author: IBM
  description: "Role that searches for a Mapping Rule (for ID lookup) and
                returns mapping_rules_obj"
  company: IBM

  license: Apache-2.0

  min_ansible_version: 2.9

  platforms:
    - name: GenericLinux
      versions:
        - all
    - name: EL
      versions:
        - all

  galaxy_tags:
    - isam
    - ibm
    - mapping_rules
    - search
    - get

dependencies:
